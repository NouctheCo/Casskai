# ===========================================
# CassKai - Variables d'environnement
# ===========================================
# Copiez ce fichier vers .env.local et remplissez avec vos vraies valeurs
# NE JAMAIS commiter .env.local ou .env avec des vraies valeurs

# ===========================================
# SUPABASE (REQUIS)
# ===========================================
# URL de votre projet Supabase
VITE_SUPABASE_URL=https://your-project.supabase.co

# Clé anonyme publique (safe pour le client)
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Alias (pour compatibilité)
VITE_SUPABASE_KEY=your-supabase-anon-key

# ===========================================
# SUPABASE - BACKEND / EDGE FUNCTIONS ONLY
# ===========================================
# ⚠️ CES CLÉS NE DOIVENT JAMAIS ÊTRE PRÉFIXÉES PAR VITE_
# ⚠️ À configurer via: supabase secrets set NOM_VARIABLE=valeur

# Service Role Key (backend uniquement, ne JAMAIS exposer côté client)
# SUPABASE_SERVICE_ROLE_KEY=eyJhbG...

# ===========================================
# STRIPE - PAIEMENTS
# ===========================================
# Clé publique Stripe (safe pour le client)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Prix IDs pour les abonnements
VITE_STRIPE_STARTER_MONTHLY_PRICE_ID=price_starter_monthly
VITE_STRIPE_STARTER_YEARLY_PRICE_ID=price_starter_yearly
VITE_STRIPE_PRO_MONTHLY_PRICE_ID=price_pro_monthly
VITE_STRIPE_PRO_YEARLY_PRICE_ID=price_pro_yearly
VITE_STRIPE_ENTERPRISE_MONTHLY_PRICE_ID=price_enterprise_monthly
VITE_STRIPE_ENTERPRISE_YEARLY_PRICE_ID=price_enterprise_yearly

# Stripe - Backend uniquement (Edge Functions)
# ⚠️ Configurer via: supabase secrets set STRIPE_SECRET_KEY=sk_test_...
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# ===========================================
# OPENAI - ASSISTANT IA (OPTIONNEL)
# ===========================================
VITE_OPENAI_API_KEY=sk-your-openai-api-key

# ===========================================
# APPLICATION - CONFIGURATION
# ===========================================
# URL de base de l'application
VITE_APP_URL=http://localhost:5173

# Environnement (development, staging, production)
VITE_APP_ENV=development

# URL de l'API backend (si séparée)
VITE_API_BASE_URL=http://localhost:3000

# ===========================================
# CONTACT & SUPPORT
# ===========================================
VITE_WHATSAPP_PHONE=+33123456789

# ===========================================
# MONITORING & ANALYTICS (PRODUCTION)
# ===========================================
# Sentry - Error tracking
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
VITE_SENTRY_ENVIRONMENT=production
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
VITE_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0.1

# ===========================================
# FEATURE FLAGS & DEBUG
# ===========================================
# Mode debug (logs verbose)
VITE_DEBUG_MODE=false

# Niveau de logs (debug, info, warn, error)
VITE_LOG_LEVEL=info

# Utiliser des services mock pour les tests
VITE_USE_MOCK_SERVICES=false

# Beta features
VITE_BETA_FEEDBACK_ENABLED=false
VITE_BETA_ANALYTICS_VERBOSE=false

# ===========================================
# TESTS E2E (PLAYWRIGHT)
# ===========================================
# ⚠️ Créer un compte de test dédié, NE PAS utiliser de vrais comptes
# ⚠️ Ces variables doivent être dans .env.test.local (non versionné)

# URL de test
# TEST_SUPABASE_URL=https://your-project.supabase.co
# TEST_SUPABASE_ANON_KEY=your-test-anon-key

# Identifiants de test
# TEST_USER_EMAIL=test@example.com
# TEST_USER_PASSWORD=TestPassword123!

# Service role pour les tests (backend uniquement)
# VITE_TEST_SUPABASE_SERVICE_ROLE_KEY=your-test-service-role-key

# ===========================================
# INTÉGRATIONS BANCAIRES - BACKEND ONLY
# ===========================================
# ⚠️ CES SECRETS NE DOIVENT JAMAIS ÊTRE PRÉFIXÉS PAR VITE_
# ⚠️ À configurer sur le serveur backend uniquement

# Bridge API (agrégation bancaire)
# REACT_APP_BRIDGE_CLIENT_ID=your-bridge-client-id
# REACT_APP_BRIDGE_CLIENT_SECRET=your-bridge-client-secret
# REACT_APP_BRIDGE_BASE_URL=https://api.bridgeapi.io
# REACT_APP_BRIDGE_WEBHOOK_SECRET=your-bridge-webhook-secret

# Budget Insight (agrégation bancaire alternative)
# REACT_APP_BUDGET_INSIGHT_CLIENT_ID=your-budget-insight-client-id
# REACT_APP_BUDGET_INSIGHT_CLIENT_SECRET=your-budget-insight-client-secret
# REACT_APP_BUDGET_INSIGHT_BASE_URL=https://api.budget-insight.com
# REACT_APP_BUDGET_INSIGHT_WEBHOOK_SECRET=your-budget-insight-webhook-secret

# ===========================================
# INTÉGRATIONS E-INVOICING - BACKEND ONLY
# ===========================================
# Chorus Pro (facturation publique)
# REACT_APP_CHORUS_PRO_LOGIN=your-chorus-pro-login
# REACT_APP_CHORUS_PRO_PASSWORD=your-chorus-pro-password

# PPF (Plateforme de Facturation)
# PPF_CLIENT_ID=your-ppf-client-id
# PPF_CLIENT_SECRET=your-ppf-client-secret

# ===========================================
# SÉCURITÉ - CHIFFREMENT AES-256-GCM
# ===========================================
# ⚠️ CRITIQUE: Clé de chiffrement pour les archives légales (7 ans)
# - Algorithme: AES-256-GCM
# - Format: Chaîne hexadécimale de 64 caractères (256 bits)
# - Génération: Utiliser la fonction generateEncryptionKey() du service
# - ⚠️ NE JAMAIS commiter cette clé
# - ⚠️ NE JAMAIS perdre cette clé (données irrécupérables)
# - ⚠️ Sauvegarder cette clé dans un gestionnaire de secrets sécurisé
VITE_ARCHIVE_ENCRYPTION_KEY=your-archive-encryption-key-64-hex-characters-here-xxxxxxxxxxxxxxxx

# Legacy (déprécié, à ne plus utiliser)
# REACT_APP_ENCRYPTION_KEY=your-32-character-encryption-key-here

# ===========================================
# EMAIL - BACKEND ONLY
# ===========================================
# SendGrid (envoi d'emails)
# SENDGRID_API_KEY=SG.your_sendgrid_api_key
# SENDGRID_FROM_EMAIL=noreply@casskai.app

# ===========================================
# DÉPLOIEMENT VPS (utilisées par les scripts)
# ===========================================
# Ces variables sont passées en paramètres aux scripts de déploiement
# Ne pas les mettre dans .env

# VPS_HOST=your-vps-ip-or-domain
# VPS_USER=your-vps-user
# VPS_PATH=/var/www/casskai.app

# ===========================================
# NOTES DE SÉCURITÉ
# ===========================================
# 1. Ne JAMAIS commiter de fichiers .env avec de vraies valeurs
# 2. Les clés préfixées VITE_ sont exposées côté client (safe)
# 3. Les clés sans VITE_ sont pour le backend uniquement
# 4. Pour Supabase Edge Functions: supabase secrets set NOM_VAR=valeur
# 5. Créer .env.local pour le développement local
# 6. Créer .env.test.local pour les tests E2E
# 7. Utiliser les secrets d'environnement en production (VPS, Vercel, etc.)

# Voir SECURITY_CONFIGURATION_GUIDE.md pour plus de détails
