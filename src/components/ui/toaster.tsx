/**
 * Legacy Toaster component — no longer renders anything.
 * The Sonner <Toaster /> is now mounted in App.tsx.
 * This file exists only so old imports don't break.
 */
export function Toaster(): null {
  return null;
}
