# E2E Test Environment Variables
# These credentials should be for a dedicated test account

# Test User Credentials (create a dedicated test user in Supabase)
TEST_USER_EMAIL=test@casskai.app
TEST_USER_PASSWORD=TestPassword123!

# Test Base URL (defaults to localhost in playwright.config.ts)
PLAYWRIGHT_BASE_URL=http://localhost:5173

# Optional: Test Company ID
TEST_COMPANY_ID=

# Optional: Supabase Test Project (if using separate test database)
# VITE_SUPABASE_URL=https://your-test-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-test-anon-key
