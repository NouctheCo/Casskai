$content = Get-Content -Path 'src\i18n\locales\en.json' -Raw
$content = $content -replace '"GÃ©nÃ©rer rapport PDF"', '"Generate PDF report"'
$content = $content -replace '"Marquer comme payÃ©"', '"Mark as paid"'
$content = $content -replace '"GÃ©rez your achats, invoices fournisseurs et paiements"', '"Manage your purchases, supplier invoices and payments"'
$content = $content -replace '"Filtres actifs"', '"Active filters"'
$content = $content -replace '"Tous les statuts"', '"All statuses"'
$content = $content -replace '"Tous les fournisseurs"', '"All suppliers"'
$content = $content -replace '"Date de dÃ©but"', '"Start date"'
$content = $content -replace '"Date de fin"', '"End date"'
$content = $content -replace '"Statut de paiement"', '"Payment status"'
$content = $content -replace '"Recherche"', '"Search"'
$content = $content -replace '"NÂ° invoice, fournisseur, description..."', '"Invoice no., supplier, description..."'
$content = $content -replace '"Fournisseur"', '"Supplier"'
$content = $content -replace '"Titre"', '"Title"'
$content = $content -replace '"PDF, JPG, PNG acceptÃ©s"', '"PDF, JPG, PNG accepted"'
$content = $content -replace '"Montant HT"', '"Amount HT"'
$content = $content -replace '"Montant TTC"', '"Amount TTC"'
$content = $content -replace '"Montants"', '"Amounts"'
$content = $content -replace '"PiÃ¨ces jointes"', '"Attachments"'
$content = $content -replace '"Montants calculÃ©s"', '"Calculated amounts"'
$content = $content -replace '"Nouvel achat"', '"New purchase"'
$content = $content -replace '"Description"', '"Description"'
$content = $content -replace '"Description de l\'achat..."', '"Purchase description..."'
$content = $content -replace '"Date d\'Ã©chÃ©ance"', '"Due date"'
$content = $content -replace '"Modifier l\'achat"', '"Edit purchase"'
$content = $content -replace '"NumÃ©ro de invoice"', '"Invoice number"'
$content = $content -replace '"FAC-2024-001"', '"INV-2024-001"'
$content = $content -replace '"SÃ©lectionnez un fournisseur"', '"Select a supplier"'
$content = $content -replace '"TVA Amount"', '"VAT amount"'
$content = $content -replace '"Taux TVA"', '"VAT rate"'
$content = $content -replace '"TÃ©lÃ©charger des fichiers"', '"Upload files"'
$content = $content -replace '"Le montant doit Ãªtre supÃ©rieur Ã  0"', '"The amount must be greater than 0"'
$content = $content -replace '"Description is required"', '"Description is required"'
$content = $content -replace '"La date d\'Ã©chÃ©ance doit Ãªtre postÃ©rieure Ã  la Purchase date"', '"The due date must be after the purchase date"'
$content = $content -replace '"La date d\'Ã©chÃ©ance est requise"', '"Due date is required"'
$content = $content -replace '"Please correct the form errors"', '"Please correct the form errors"'
$content = $content -replace '"Le numÃ©ro de invoice est requis"', '"Invoice number is required"'
$content = $content -replace '"La Purchase date est requise"', '"Purchase date is required"'
$content = $content -replace '"Supplier is required"', '"Supplier is required"'
$content = $content -replace '"Aucun achat pour le moment"', '"No purchases yet"'
$content = $content -replace '"CrÃ©ez your first achat pour commencer"', '"Create your first purchase to get started"'
$content = $content -replace '"Achat crÃ©Ã© avec succÃ¨s"', '"Purchase created successfully"'
$content = $content -replace '"ÃŠtes-vous sÃ»r de vouloir supprimer cet achat ?"','"Are you sure you want to delete this purchase?"'
$content = $content -replace '"Achat supprimÃ© avec succÃ¨s"', '"Purchase deleted successfully"'
$content = $content -replace '"Delete Purchase"', '"Delete Purchase"'
$content = $content -replace '"Une invoice avec ce numÃ©ro existe dÃ©jÃ "', '"An invoice with this number already exists"'
$content = $content -replace '"Export rÃ©alisÃ© avec succÃ¨s"', '"Export completed successfully"'
$content = $content -replace '"Marquer cette invoice comme payÃ©e ?"','"Mark this invoice as paid?"'
$content = $content -replace '"Facture marquÃ©e comme payÃ©e"', '"Invoice marked as paid"'
$content = $content -replace '"Achat mis Ã  jour avec succÃ¨s"', '"Purchase updated successfully"'
$content = $content -replace '"Attention"', '"Warning"'
$content = $content -replace '"Paiements en retard"', '"Overdue payments"'
$content = $content -replace '"Factures en retard"', '"Overdue invoices"'
$content = $content -replace '"Paiements en attente"', '"Pending payments"'
$content = $content -replace '"Invoices non payÃ©es"', '"Unpaid invoices"'
$content = $content -replace '"Amount total"', '"Total amount"'
$content = $content -replace '"Total purchase amount"', '"Total purchase amount"'
$content = $content -replace '"Total achats"', '"Total purchases"'
$content = $content -replace '"Nombre total d\'achats"', '"Total number of purchases"'
$content = $content -replace '"Urgent"', '"Urgent"'
$content = $content -replace '"En retard"', '"Overdue"'
$content = $content -replace '"PayÃ©"', '"Paid"'
$content = $content -replace '"En attente"', '"Pending"'
$content = $content -replace '"Amount HT"', '"Amount HT"'
$content = $content -replace '"Amount TTC"', '"Amount TTC"'
$content = $content -replace '"Date"', '"Date"'
$content = $content -replace '"Description"', '"Description"'
$content = $content -replace '"Ã‰chÃ©ance"', '"Due date"'
$content = $content -replace '"NÂ° Facture"', '"Invoice No."'
$content = $content -replace '"Status"', '"Status"'
$content = $content -replace '"Fournisseur"', '"Supplier"'
$content = $content -replace '"Gestion des Achats"', '"Purchase Management"'
$content | Set-Content -Path 'src\i18n\locales\en.json'
