# Example deployment configuration for CassKai
# Copy to .env.deploy and adjust values

# VPS connection
VPS_HOST=89.116.111.88
VPS_USER=root

# Paths on VPS
VPS_FRONTEND_PATH=/var/www/casskai.app
VPS_BACKEND_PATH=/var/www/casskai.app/api

# Domain (for tests)
DOMAIN_NAME=casskai.app

# PM2 process name
PM2_PROCESS_NAME=casskai-api
